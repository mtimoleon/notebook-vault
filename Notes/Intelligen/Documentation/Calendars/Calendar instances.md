**Instances boundaries truncation**

During calendar instances creation, we use the  ==​==`trancateBoundaries`==​==  method param having a default value to true. With this parameter we denote if we want the created calendar instances to be trancated usually to day start/end.